# Troubleshooting

## TLS fingerprint disconnects on ESP8266

With `ASYNC_TCP_SSL_BEARSSL`, fingerprint mismatches are rejected during the TLS handshake and surface as `AsyncMqttClientDisconnectReason::TLS_BAD_FINGERPRINT`.
